・関数従属（Functional Dependency）
データベース（RDB）などにおいて、ある属性（列）の値を決めると、
別の属性の値が一意に（ただ一つに）決まる関係性

例）社員番号（X）が決まると、氏名（Y）が1つに決まる。

・推移的に関数従属
データベース（RDB）において、ある列の値が主キーのような
直接的な要素ではなく、別の列（中間的な列）を経由して
間接的に一意に決まる関係

例）社員が決まると部署が決まり、部署長が決まるので、
　　社員が決まれば部署長が決まる。